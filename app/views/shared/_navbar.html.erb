<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container">
    <div class="row w-100">
      <div class="col-lg-6 offset-lg-3 d-flex justify-content-between align-items-center">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <div class="me-2">
            <%= render partial: "shared/logo" %>
          </div>
          <span class="fw-bold fs-4" style="font-family: 'Cinzel', serif;">Shinar</span>
        </a>
        <div>
          <% if current_user.present? %>
            <%= link_to edit_user_path(current_user), class: "text-decoration-none" do %>
              <i class="fas fa-user me-1"></i> Profile
            <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
