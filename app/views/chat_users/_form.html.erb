<%= form_with(model: chat_user) do |form| %>
  <% if chat_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chat_user.errors.count, "error") %> prohibited this chat_user from being saved:</h2>

      <ul>
        <% chat_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :chat_id, style: "display: block" %>
    <%= form.text_field :chat_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
